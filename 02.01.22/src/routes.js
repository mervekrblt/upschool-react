import About from "./components/about";
import Home from "./components/home";

const routes = [
  {id:1, path: '/', element: Home, isNav:true},
  {id:1, path: '/about', element: About, isNav:true}
]

export default routes